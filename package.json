{
  "name": "auto-download-image",
  "version": "1.0.0",
  "description": "自动检测并下载笔记中的网络图片到本地，支持自定义保存位置、命名格式和路径类型",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "lint": "eslint ."
  },
  "keywords": [
    "obsidian",
    "plugin",
    "image",
    "download",
    "network",
    "offline",
    "markdown",
    "auto"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.25.5",
    "globals": "^14.0.0",
    "tslib": "^2.8.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.35.1",
    "@eslint/js": "^9.30.1",
    "jiti": "^2.6.1"
  },
  "dependencies": {
    "obsidian": "^1.7.5"
  }
}
